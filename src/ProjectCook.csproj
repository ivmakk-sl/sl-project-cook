<Project Sdk="Microsoft.NET.Sdk">

  <!-- Build settings and the common game references live in Directory.Build.props at the repo root.
       This file names the plugin and adds the assemblies that only this mod uses. -->
  <PropertyGroup>
    <AssemblyName>ProjectCook</AssemblyName>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <!-- The cooking window is a Vuplex web view, and its state uses R3 and ObservableCollections types. -->
  <ItemGroup>
    <_Ref Include="$(BepInExInterop)\Vuplex.WebView.dll" />
    <_Ref Include="$(BepInExInterop)\R3.dll" />
    <_Ref Include="$(BepInExInterop)\ObservableCollections.dll" />
    <Reference Include="@(_Ref)">
      <HintPath>%(FullPath)</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

</Project>
